<div class="d-grid gap-2 d-md-block mb-3">
    <a href="{{ path('edr_parent_sante_fiche_show', { 'uuid': enfant.uuid }) }}" role="button"
       class="btn btn-primary">
        <i class="ti ti-heartbeat"></i> Fiche santé</a>
    <a href="{{ path('edr_parent_enfant_index') }}" class="btn btn-primary" role='button'><i
                class="ti ti-list"></i>
        Retour à la Liste</a>
</div>
