{% extends '@AcMarcheEdrParent/layout.html.twig' %}

{% block body %}
    <div class="card">

        <h3 class="card-header">{{ enfant }}
            <div class="float-end">
                {% include '@AcMarcheEdr/commun/_archive.html.twig' with {'entity':enfant} %}
            </div>
        </h3>
        <div class="card-body">
            {% if ficheSanteComplete == false %}
                <div class="alert alert-danger">
                    Attention la fiche santé n'est pas complète, veuillez la remplir.
                </div>
            {% endif %}
            {% include '@AcMarcheEdrParent/enfant/_actions.html.twig' %}

            <div class="row">
                <div class="col-12 col-lg-3">
                    {% include '@AcMarcheEdr/commun/enfant/_photo.html.twig' %}
                </div>
                <div class="mt-3 col-12 col-lg-9">
                    {% include '@AcMarcheEdr/commun/enfant/_identite.html.twig' %}
                </div>
            </div>
            <br/>
        </div>
        <div class="card-footer text-muted mt-3">

        </div>
    </div>
    <br/>
    {% include '@AcMarcheEdrParent/enfant/_presences.html.twig' %}
    {% include '@AcMarcheEdrParent/enfant/_plaines.html.twig' %}
    {% include '@AcMarcheEdrParent/enfant/_accueils.html.twig' %}

{% endblock %}
