{% extends '@AcMarcheEdrEcole/layout.html.twig' %}

{% block body %}

    <div class="card mb-3">
        <div class="card-header">
            <h3>Fiche santé de {{ enfant }}</h3>
        </div>
        <div class="card-body">

            <a href="{{ path('edr_commun_export_sante_fiche_pdf', { 'uuid': enfant.uuid }) }}" role="button"
               class="btn btn-primary"><i class="far fa-file-pdf"></i> Pdf</a>
            <br/> <br/>


            {% include '@AcMarcheEdr/sante/_detail.html.twig' %}
        </div>
    </div>

{% endblock %}
