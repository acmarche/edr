<div class="d-grid gap-2 d-md-block mb-3">

    {% include '@AcMarcheEdr/ecole/enfant/_btn_add_accueil.html.twig' %}

    <a href="{{ path('edr_ecole_sante_fiche_show', { 'uuid': enfant.uuid }) }}" role="button"
       class="btn btn-primary"><i class="ti ti-heartbeat"></i> Fiche santé</a>

    <a href="{{ path('edr_ecole_accueil_retard', { 'uuid': enfant.uuid }) }}" role="button"
       class="btn btn-primary"><i class="ti ti-hourglass-half"></i> Retard</a>

    <a href="{{ path('edr_ecole_enfant_index') }}" class="btn btn-primary" role='button'><i
                class="ti ti-list"></i>
        Retour à la Liste</a>
</div>
