<div class="btn-toolbar">

    <div class="btn-group mb-2 mr-2 ">
        <a href="{{ path('edr_commun_export_sante_fiche_pdf', { 'uuid': enfant.uuid }) }}" role="button"
           class="btn btn-primary">
            <i class="ti ti-heartbeat"></i> Fiche santé</a>
    </div>
    <div class="btn-group mb-2 ">
        <a href="{{ path('edr_animateur_enfant_index') }}" class="btn btn-primary" role='button'><i
                    class="ti ti-list"></i>
            Retour à la Liste</a>
    </div>
</div>

