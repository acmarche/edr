{% extends '@AcMarcheEdrAdmin/layout.html.twig' %}

{% block body %}

    <div class="card mb-3">
        <div class="card-header">
            <h3>{{ tuteur }}</h3>
        </div>
        <div class="card-body">

            {{ include('@AcMarcheEdrAdmin/tuteur/_form.html.twig') }}

        </div>
        <div class="card-footer">
            <a href="{{ path('edr_admin_tuteur_show',{'id':tuteur.id}) }}" class="mt-4">
                <i class="fas fa-long-arrow-alt-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
