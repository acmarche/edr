{% extends '@AcMarcheEdrAdmin/layout.html.twig' %}

{% block body %}
    <div class="card mb-3">
        <div class="card-header">
            <h3>Nouveau parent</h3>
        </div>
        <div class="card-body">
            {{ include('@AcMarcheEdrAdmin/tuteur/_form.html.twig') }}
        </div>
        <div class="card-footer">
            <a href="{{ path('edr_admin_tuteur_index') }}">
                <span class="fas fa-list"></span> Annuler
            </a>
        </div>
    </div>
{% endblock %}
