<a href="{{ path('edr_admin_facture_complement_edit', { 'id': factureComplement.id }) }}" role="button"
   class="btn btn-primary"><i
            class="ti ti-edit"></i> Modifier</a>

<a href="{{ path('edr_admin_facture_show', {'id': facture.id}) }}" class="btn btn-primary" role='button'><i
            class="ti ti-list"></i>
    Retour à la facture</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> Supprimer
</button>

<br/><br/>
